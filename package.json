{
  "name": "apothera-backend",
  "version": "1.0.0",
  "description": "Backend proxy that safely holds the API key for the Apothera medicine-info website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
